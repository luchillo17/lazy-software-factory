/**
 * Default Cursor SDK model for Host / local ADW when no model is passed
 * and ADW_MODEL / CURSOR_MODEL are unset.
 *
 * SDK id for Cursor Grok 4.5 (evals + local catalog).
 */
export const DEFAULT_LOCAL_ADW_MODEL = "grok-4.5";
